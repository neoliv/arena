### Prompts

- On the game details page. Show charts with: time per ply (bar?). eval per ply (bar?) NpS per ply, ... all available metrics for this game. Use a slightly colored backgroud to mark book/middle/ES engine phases whan avallable. On the bottom axis put the ply number and move played one below the other. eg: C3 and 38 below.

- On the player page I see that edax players show "unknown" for the created date. Use the date of the git version pulled to build the engine binary. btw does edax have a --version that can be used in manifest or other places where it's useful? (same question for all other engines)

- build new complete coach-adapters/ for other AIs in agent/othello-refs/ (eg: egaroucid). As for edax, create 4 playrs of various strength.

arena: Explain how the stats are affected to the proper player.
explain the workflow from registration to match maker staring a game and stats collected by arena.


Could the edax adapter collect more stats? (like for the darw adapter). Show recommendations, don't implement until I say so.


### Maybe

- outside the DB, arena should also build a file containing only games (with useful meta-data like players, time allowed, final score,... use WTHOR format and usual meta-data)



