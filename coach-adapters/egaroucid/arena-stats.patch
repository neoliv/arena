diff --git a/src/console/gtp_command.hpp b/src/console/gtp_command.hpp
index ba1e6e4..3646f9b 100644
--- a/src/console/gtp_command.hpp
+++ b/src/console/gtp_command.hpp
@@ -215,19 +215,18 @@ void gtp_genmove(int id, std::string arg, Board_info *board, State *state, Optio
         std::cout << gtp_head(id) << " PASS" << GTP_ENDL;
         return;
     }
-    int policy = ai_with_settings(board->board, options, true, options->show_log).policy;
+    Search_result result = ai_with_settings(board->board, options, true, options->show_log);
+    int policy = result.policy;
     Flip flip;
     calc_flip(&flip, &board->board, policy);
     board->board.move_board(&flip);
     board->player ^= 1;
     board->boards.emplace_back(board->board.copy());
     board->players.emplace_back(board->player);
-    //if (board->board.get_legal() == 0ULL) {
-    //    board->board.pass();
-    //    board->player ^= 1;
-    //}
+    int empties = HW2 - board->board.n_discs();
+    std::cout << "# arena-stats v1: {\"nodes\":" << result.nodes << ",\"depth\":" << result.depth << ",\"score\":" << result.value << ",\"empties\":" << empties << ",\"time_ms\":" << (result.time / 1000) << ",\"timeout\":false}" << GTP_ENDL;
     std::cout << gtp_head(id) << " " << gtp_idx_to_coord(policy) << GTP_ENDL;
-}
+    }
 
 void gtp_rules_game_id(int id) {
     std::cout << gtp_head(id) << " " << GTP_RULE_ID << GTP_ENDL;
@@ -372,9 +371,12 @@ void gtp_reg_genmove(int id, std::string arg, Board_info *board, State *state, O
         std::cout << gtp_head(id) << " PASS" << GTP_ENDL;
         return;
     }
-    int policy = ai_with_settings(board->board, options, true, options->show_log).policy;
+    Search_result reg_result = ai_with_settings(board->board, options, true, options->show_log);
+    int policy = reg_result.policy;
+    int reg_empties = HW2 - board->board.n_discs();
+    std::cout << "# arena-stats v1: {\"nodes\":" << reg_result.nodes << ",\"depth\":" << reg_result.depth << ",\"score\":" << reg_result.value << ",\"empties\":" << reg_empties << ",\"time_ms\":" << (reg_result.time / 1000) << ",\"timeout\":false}" << GTP_ENDL;
     std::cout << gtp_head(id) << " " << gtp_idx_to_coord(policy) << GTP_ENDL;
-}
+    }
 
 void gtp_list_games(int id) {
     std::cout << gtp_head(id) << " " << GTP_RULE_ID << GTP_ENDL;
diff --git a/src/console/option.hpp b/src/console/option.hpp
index b2e70d9..3c8bd16 100644
--- a/src/console/option.hpp
+++ b/src/console/option.hpp
@@ -41,7 +41,9 @@ struct Options {
     std::string eval_file;
     bool nobook;
     int mode;
-    bool gtp;
+    int depth_override;
+    int es_override;
+        bool gtp;
     bool quiet;
     int time_allocated_seconds; // -1 (TIME_NOT_ALLOCATED): not allocated
     bool ponder;
@@ -157,6 +159,20 @@ Options get_options(std::vector<Commandline_option> commandline_options, std::st
             std::cerr << "[ERROR] level argument out of range" << std::endl;
         }
     }
+    res.depth_override = -1;
+    if (find_commandline_option(commandline_options, ID_DEPTH)) {
+        std::vector<std::string> arg = get_commandline_option_arg(commandline_options, ID_DEPTH);
+        try { res.depth_override = std::stoi(arg[0]); } catch (...) {}
+    }
+    res.es_override = -1;
+    if (find_commandline_option(commandline_options, ID_ES)) {
+        std::vector<std::string> arg = get_commandline_option_arg(commandline_options, ID_ES);
+        try { res.es_override = std::stoi(arg[0]); } catch (...) {}
+    }
+    // Apply overrides to globals
+    g_mid_depth_override = res.depth_override;
+    g_end_depth_override = res.es_override;
+
     res.use_depth_prob_ranges = false;
     res.depth_prob_ranges.clear();
     if (find_commandline_option(commandline_options, ID_DEPTH_PROB_RANGE)) {
@@ -272,7 +288,7 @@ Options get_options(std::vector<Commandline_option> commandline_options, std::st
             std::cerr << "[ERROR] mode argument out of range" << std::endl;
         }
     }
-    res.gtp = find_commandline_option(commandline_options, ID_GTP);
+    res.gtp = true; // Adapter: always GTP mode by default
     res.quiet = find_commandline_option(commandline_options, ID_QUIET);
     res.time_allocated_seconds = TIME_NOT_ALLOCATED;
     if (find_commandline_option(commandline_options, ID_TIME_ALLOCATE)) {
