### Prompts

- The "games" page still does not update automatically. (3rd try, so be thorough in you analysis.)





### redesign?

- Regarding the matchmaker vs coach allocation issue. This is still not working as intended. (I often see 6/6 with 0 game in progress and 0% CPU on the host). We need to discuss the flow to understand the catch. Don't implement yet, only analyse and recommend.
- Is it True that a game can run between AIs managed by different coaches (probably on different hosts). Is arena relaying the moves or is it P2P?
- How is the match-maker (MM from now on) deciding that a new match between two engines is required?
- How is the MM informing the coaches the match is ready and should be processed (push? poll by the coaches?)
- How is the MM deciding a coach should have enough resource for a given match? (does it take into account the core allocation preference of the player?)

Design ideas to comment upon:
- The MM creates and update a list of 'desirable' games to best fit the current requirements (Elo Ranking, Tournament, Blitz tournament, ...).
- The MM use this list of games to build an ordered list of required players for these games and post it so the coaches can read it.
- The MM receives



I think there is something fishy anyway. The 'core occupation' state the
  coach (should) see and the one shown by MM/arena are not the same. WHen no
  engine is running, no game in progress, the MM/arena should see 0/6 not 6/6.
  How could we redesign the flow to make sure the final word is the coach that
  really knows what resources are used? Thin, analyse with as many agents as you  need. Offer new designs with pro/cons. 




arena: Explain how the stats are affected to the proper player.
explain the workflow from registration to match maker staring a game and stats collected by arena.


Could the edax adapter collect more stats? (like for the darw adapter). Show recommendations, don't implement until I say so.


### Maybe
- On the game page, I see only the last 100 games. Would it be hard to have the full list? even if its 100k games? What are the solutions for this, give recommendations (no implementation yet)

- Add a page to manage the match maker mode (Elo ranking, Tournament, Blitz tournament, ...) This will change the way the list of desirable games is built. This page could show the list of games that the MM will try to run in the near future.
- outside the DB, arena should also build a file containing only games (with useful meta-data like players, time allowed, final score,... use WTHOR format and usual meta-data)



